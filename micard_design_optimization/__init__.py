"""MICARD — multi-objective design-space exploration for modular serial robotic
arms. See README.md for the pipeline; micard_design_optimization.run.run() is
the library entry point (the `main.py` at the repo root is a thin CLI shim
over it, for standalone/dev use).
"""
